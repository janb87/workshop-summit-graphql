### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


type Mutation {
  createDraft(authorEmail: String!, content: String!, title: String!): Post!
  publish(postId: Int!): Post!
  signupUser(data: NewUser): User!
}

input NewUser {
  email: String!
  name: String!
  role: UserRole!
}

type Post {
  author: User!
  content: String!
  id: Int!
  published: Boolean!
  title: String!
}

type Query {
  hello(name: String): String!
  post(id: Int!): Post
  posts(authorEmail: String, published: Boolean, searchString: String): [Post!]!
  user(id: Int!): User
  users: [User!]
}

type User {
  email: String!
  id: Int!
  name: String!
  role: UserRole
}

enum UserRole {
  Admin
  Visitor
}
